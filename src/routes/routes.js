
import views from "../views"

const routes = [
    /**
     * Add your own routes
     */
    {
        path : '/',
        name : 'home',
        component : views.Home,
        props : true,
        meta : {}
    }
]

export default routes


